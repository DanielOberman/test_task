services:
  # pythonapp:
  #   build: ./python/
  #   command: sh -c "sleep 10s ; uvicorn main:app --host 0.0.0.0 --port 8000"
  #   depends_on:
  #     - mysql

  mysql:
    build: ./mysql/
    restart: always
    environment:
      MYSQL_DATABASE: 'db'
      MYSQL_ROOT_PASSWORD: 'root'
    ports:
      - '3306:3306'